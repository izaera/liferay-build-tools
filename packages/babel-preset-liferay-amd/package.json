{
  "name": "babel-preset-liferay-amd",
  "version": "0.4.0",
  "description": "Babel preset for bundling AMD Liferay projects.",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel --source-maps -D -d lib src",
    "prepublish": "babel --source-maps -D -d lib src"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "babel-plugin-name-amd-modules": "^0.4.0",
    "babel-plugin-namespace-amd-define": "^0.4.0",
    "babel-plugin-normalize-requires": "^0.4.0"
  }
}
