{
  "name": "liferay-npm-bundler-plugin-inject-package-dependencies",
  "version": "0.5.0",
  "description": "A liferay-npm-bundler plugin to force injection of dependencies in package.json files.",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel --source-maps -D -d lib src",
    "prepublish": "babel --source-maps -D -d lib src"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1"
  }
}
